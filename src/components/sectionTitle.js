const sectionTitle = (titulo = 'titulo de la seccion') => {
  return `<div class='w-full p-2 bg-[#3C44FF]'>
  <h2 class="text-xl text-slate-50 ">${titulo}</h2>
  </div>`;
};

export default sectionTitle;
